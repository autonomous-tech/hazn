---
phase: 02-model-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hazn_platform/hazn_platform/orchestrator/models.py
  - hazn_platform/hazn_platform/core/models.py
  - hazn_platform/hazn_platform/users/models.py
  - hazn_platform/hazn_platform/workspace/share_models.py
autonomous: true
requirements: [STRP-02, STRP-03, STRP-07, STRP-10]

must_haves:
  truths:
    - "HITLItem model class no longer exists in orchestrator/models.py"
    - "WorkflowPhaseOutput has html_content and markdown_source TextFields"
    - "Agency model has singleton enforcement via save() override"
    - "Enterprise fields (turn_count, churned_at, deletion_*, tool_preferences, agency_role) are removed from all models"
    - "WorkflowRun.conflict_log JSONField is preserved (kept as generic run notes)"
    - "WorkflowRun.langfuse_trace_id is preserved (actively used by tracing.py)"
    - "Django makemigrations generates clean migration(s) reflecting all model changes"
  artifacts:
    - path: "hazn_platform/hazn_platform/orchestrator/models.py"
      provides: "WorkflowPhaseOutput with html_content/markdown_source, no HITLItem, no turn_count fields"
      contains: "html_content"
    - path: "hazn_platform/hazn_platform/core/models.py"
      provides: "Agency singleton with save() override, no lifecycle fields, no tool_preferences"
      contains: "def save"
    - path: "hazn_platform/hazn_platform/users/models.py"
      provides: "User model without agency_role field or AgencyRole class"
  key_links:
    - from: "hazn_platform/hazn_platform/core/models.py"
      to: "Agency singleton"
      via: "save() override with existence check"
      pattern: "Agency\\.objects\\.exists"
    - from: "hazn_platform/hazn_platform/orchestrator/models.py"
      to: "WorkflowPhaseOutput"
      via: "html_content and markdown_source fields"
      pattern: "html_content.*TextField"
---

<objective>
Modify all Django model files for Phase 2 simplification: delete HITLItem model class, add deliverable rendering fields to WorkflowPhaseOutput, remove enterprise fields from WorkflowRun/WorkflowAgent/Agency/EndClient/User, enforce Agency singleton, handle ShareLink FK, and generate the coordinated Django migration.

Purpose: Establish the simplified model layer that all subsequent API cleanup depends on. Models must be correct before views/serializers/admin can be updated.
Output: Updated model files with all field changes and a Django migration that applies cleanly.
</objective>

<execution_context>
@/Users/rizwanqaiser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rizwanqaiser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-simplification/02-CONTEXT.md
@.planning/phases/02-model-simplification/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify all model files -- delete HITLItem, add PhaseOutput fields, remove enterprise fields, Agency singleton</name>
  <files>
    hazn_platform/hazn_platform/orchestrator/models.py,
    hazn_platform/hazn_platform/core/models.py,
    hazn_platform/hazn_platform/users/models.py
  </files>
  <action>
Read all three model files first, then make these changes:

**orchestrator/models.py:**
1. DELETE the entire HITLItem class (including its Status/TriggerType enums and all fields). The research identified this at lines 134-178 but verify current line numbers.
2. ADD to WorkflowPhaseOutput class:
   - `html_content = models.TextField(blank=True, default="")` -- migrated from qa.Deliverable
   - `markdown_source = models.TextField(blank=True, default="")` -- migrated from qa.Deliverable
3. REMOVE `WorkflowRun.turn_count` field (dead after Phase 1)
4. REMOVE `WorkflowAgent.turn_count` field (cost tracking via total_tokens/total_cost only)
5. KEEP `WorkflowRun.conflict_log` JSONField (per user decision: kept as generic run notes)
6. KEEP `WorkflowRun.langfuse_trace_id` (actively used by tracing.py)
7. REMOVE any import of HITLItem-related code if present at module level

**core/models.py:**
1. REMOVE `Agency.tool_preferences` JSONField (stores hitl_config and conflict resolution settings -- both deleted)
2. REMOVE `Agency.churned_at` DateTimeField (GDPR lifecycle -- dead)
3. REMOVE `Agency.deletion_notified_at` DateTimeField (GDPR lifecycle -- dead)
4. KEEP `Agency.house_style` and `Agency.methodology` (feed agent system prompts)
5. ADD Agency singleton enforcement via save() override:
   ```python
   def save(self, *args, **kwargs):
       if not self.pk and Agency.objects.exists():
           raise ValueError("Only one Agency instance is allowed.")
       super().save(*args, **kwargs)

   @classmethod
   def load(cls):
       """Load the singleton Agency, creating default if needed."""
       obj, _ = cls.objects.get_or_create(
           defaults={"name": "Hazn", "slug": "hazn"}
       )
       return obj
   ```
6. REMOVE `EndClient.deletion_requested_at` DateTimeField (GDPR lifecycle -- dead)
7. REMOVE `EndClient.deletion_scheduled_at` DateTimeField (GDPR lifecycle -- dead)

**users/models.py:**
1. REMOVE the `AgencyRole` class/enum (research identified lines 15-17)
2. REMOVE the `agency_role` field from the User model (research identified lines 32-36)

IMPORTANT: Do NOT touch views, serializers, URLs, admin, or any other files in this task. Only model files.
  </action>
  <verify>
    <automated>cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && python -c "
import django; import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings.local')
django.setup()
from hazn_platform.orchestrator.models import WorkflowPhaseOutput, WorkflowRun, WorkflowAgent
from hazn_platform.core.models import Agency, EndClient
from hazn_platform.users.models import User
# Verify HITLItem is gone
try:
    from hazn_platform.orchestrator.models import HITLItem
    print('FAIL: HITLItem still importable')
except ImportError:
    print('PASS: HITLItem removed')
# Verify new fields exist
assert hasattr(WorkflowPhaseOutput, 'html_content'), 'FAIL: missing html_content'
assert hasattr(WorkflowPhaseOutput, 'markdown_source'), 'FAIL: missing markdown_source'
print('PASS: PhaseOutput fields added')
# Verify removed fields
assert not hasattr(WorkflowRun, 'turn_count'), 'turn_count still on WorkflowRun'
assert not hasattr(WorkflowAgent, 'turn_count'), 'turn_count still on WorkflowAgent'
assert not hasattr(Agency, 'tool_preferences'), 'tool_preferences still on Agency'
assert not hasattr(Agency, 'churned_at'), 'churned_at still on Agency'
assert not hasattr(EndClient, 'deletion_requested_at'), 'deletion_requested_at still on EndClient'
print('PASS: Enterprise fields removed')
# Verify singleton
assert hasattr(Agency, 'load'), 'Agency.load() classmethod missing'
print('PASS: Agency singleton methods exist')
# Verify kept fields
assert hasattr(WorkflowRun, 'conflict_log'), 'conflict_log should be kept'
assert hasattr(WorkflowRun, 'langfuse_trace_id'), 'langfuse_trace_id should be kept'
assert hasattr(Agency, 'house_style'), 'house_style should be kept'
print('PASS: Kept fields preserved')
print('ALL CHECKS PASSED')
"</automated>
  </verify>
  <done>All three model files updated: HITLItem deleted, PhaseOutput has html_content/markdown_source, enterprise fields removed from 5 models, Agency has singleton save()/load(), User has no agency_role.</done>
</task>

<task type="auto">
  <name>Task 2: Handle ShareLink FK dependency, generate coordinated Django migration</name>
  <files>
    hazn_platform/hazn_platform/workspace/share_models.py
  </files>
  <action>
Read workspace/share_models.py and workspace/share_views.py first to understand the ShareLink model.

**ShareLink decision (Claude's discretion per CONTEXT.md):**
The research recommends deleting ShareLink entirely since it is a QA-era feature. If share functionality is needed in v3.0, it can be re-created pointing to WorkflowPhaseOutput. Delete the ShareLink model from share_models.py. If share_models.py contains ONLY the ShareLink model, the entire file can be emptied (keep just imports needed by Django or an empty file).

NOTE: Do NOT delete share_views.py yet -- that happens in Plan 02 with the rest of the API cleanup. If share_views.py imports ShareLink, it will break temporarily. This is acceptable because Plan 02 will delete share_views.py.

**Generate migration:**
Run `cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && python manage.py makemigrations` to generate the coordinated migration(s). Django will automatically handle FK dependency ordering across apps.

If makemigrations fails because of import issues (e.g., views still referencing deleted models), that is expected -- the migration system only needs models.py files to be correct, not views. If it truly fails, investigate and fix the model-level issue.

After generating migration(s), verify they look correct by reading them. Then verify they would apply cleanly:
`cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && python manage.py migrate --check` (should show unapplied migrations exist, which is expected since we have not run migrate yet).

Do NOT run `python manage.py migrate` -- we are accumulating changes and will squash in Plan 02.
  </action>
  <verify>
    <automated>cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && python manage.py makemigrations --check 2>&1 || echo "Migrations pending (expected -- not yet squashed)"</automated>
  </verify>
  <done>ShareLink model handled (deleted or re-pointed), Django migration(s) generated covering all model changes from Task 1 and Task 2. Migration files exist in the relevant app migration directories.</done>
</task>

</tasks>

<verification>
1. `python -c "from hazn_platform.orchestrator.models import WorkflowPhaseOutput; print('html_content' in [f.name for f in WorkflowPhaseOutput._meta.get_fields()])"` returns True
2. `python -c "from hazn_platform.orchestrator.models import HITLItem"` raises ImportError
3. `python manage.py makemigrations --dry-run` shows no additional migrations needed beyond what was generated
4. Agency.load() classmethod exists and Agency.save() includes singleton check
</verification>

<success_criteria>
- HITLItem model class fully removed from orchestrator/models.py
- WorkflowPhaseOutput has html_content and markdown_source fields
- Enterprise fields removed from WorkflowRun, WorkflowAgent, Agency, EndClient, User
- Agency has singleton save()/load() pattern
- ShareLink FK dependency resolved (model deleted)
- Django migration(s) generated and ready for squashing in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-simplification/02-01-SUMMARY.md`
</output>
