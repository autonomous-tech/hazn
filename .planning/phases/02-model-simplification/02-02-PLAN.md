---
phase: 02-model-simplification
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - hazn_platform/hazn_platform/workspace/views.py
  - hazn_platform/hazn_platform/workspace/serializers.py
  - hazn_platform/hazn_platform/workspace/urls.py
  - hazn_platform/hazn_platform/workspace/filters.py
  - hazn_platform/hazn_platform/workspace/share_views.py
  - hazn_platform/hazn_platform/orchestrator/admin.py
  - hazn_platform/hazn_platform/orchestrator/executor.py
  - hazn_platform/hazn_platform/orchestrator/tasks.py
  - hazn_platform/hazn_platform/orchestrator/api/views.py
  - hazn_platform/hazn_platform/orchestrator/api/urls.py
  - hazn_platform/hazn_platform/orchestrator/api/serializers.py
  - config/settings/base.py
autonomous: true
requirements: [STRP-02, STRP-03, STRP-07, STRP-10]

must_haves:
  truths:
    - "All IsAgencyMember references replaced with IsAuthenticated in all viewsets"
    - "No Python file imports HITLItem, Deliverable (qa), conflict_detector, hitl module, or IsAgencyMember"
    - "QA app directory is fully deleted and removed from INSTALLED_APPS"
    - "conflict_detector.py, hitl.py, permissions.py are deleted"
    - "executor.py has TODO comments marking dead imports for Phase 4 cleanup"
    - "All dead test files deleted (10 test files)"
    - "All migrations squashed to fresh 0001_initial.py per app"
    - "python manage.py makemigrations --check produces no new migrations"
    - "Remaining test files updated to remove references to deleted models/fields"
  artifacts:
    - path: "hazn_platform/hazn_platform/workspace/views.py"
      provides: "Viewsets with IsAuthenticated, no HITL/Deliverable views"
      contains: "IsAuthenticated"
    - path: "hazn_platform/hazn_platform/orchestrator/executor.py"
      provides: "Dead imports commented out with TODO(Phase 4) markers"
      contains: "TODO(Phase 4)"
    - path: "hazn_platform/hazn_platform/workspace/serializers.py"
      provides: "Serializers without HITLItem/Deliverable/turn_count references"
  key_links:
    - from: "hazn_platform/hazn_platform/workspace/views.py"
      to: "rest_framework.permissions.IsAuthenticated"
      via: "permission_classes on all viewsets"
      pattern: "IsAuthenticated"
    - from: "config/settings/base.py"
      to: "INSTALLED_APPS"
      via: "qa app removed from LOCAL_APPS"
      pattern: "LOCAL_APPS"
---

<objective>
Clean up all API layer code (views, serializers, URLs, filters, admin, tasks, permissions), delete the QA app directory and dead source/test files, update remaining tests, then squash all migrations to fresh 0001_initial.py per app for a clean v3.0 schema.

Purpose: Complete the Phase 2 model simplification by removing all references to deleted models/features from the API layer, deleting dead code, and producing clean initial migrations.
Output: A Django codebase where all enterprise features are fully removed, migrations are clean, and makemigrations --check passes.
</objective>

<execution_context>
@/Users/rizwanqaiser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rizwanqaiser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-simplification/02-CONTEXT.md
@.planning/phases/02-model-simplification/02-RESEARCH.md
@.planning/phases/02-model-simplification/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up API layer, delete dead source files and QA app, comment executor imports</name>
  <files>
    hazn_platform/hazn_platform/workspace/views.py,
    hazn_platform/hazn_platform/workspace/serializers.py,
    hazn_platform/hazn_platform/workspace/urls.py,
    hazn_platform/hazn_platform/workspace/filters.py,
    hazn_platform/hazn_platform/workspace/share_views.py,
    hazn_platform/hazn_platform/orchestrator/admin.py,
    hazn_platform/hazn_platform/orchestrator/executor.py,
    hazn_platform/hazn_platform/orchestrator/tasks.py,
    hazn_platform/hazn_platform/orchestrator/api/views.py,
    hazn_platform/hazn_platform/orchestrator/api/urls.py,
    hazn_platform/hazn_platform/orchestrator/api/serializers.py,
    config/settings/base.py
  </files>
  <action>
Read ALL files listed above before making changes. The research document has exact line numbers and specific items to change in each file.

**workspace/views.py:**
- DELETE HITLItemViewSet class (research: lines 314-381)
- DELETE DeliverableViewSet class (research: lines 384-467)
- DELETE DeliverableHTMLView class (research: lines 507-535)
- REPLACE all `IsAgencyMember` with `IsAuthenticated` in the remaining 9 viewsets
- REMOVE imports: HITLItem, Deliverable, approve_hitl_item, reject_hitl_item, HITLItemFilter, DeliverableFilter, HITLItemSerializer, DeliverableSerializer, ShareLink, IsAgencyMember
- ADD import: `from rest_framework.permissions import IsAuthenticated`
- UPDATE DashboardView.get(): remove pending_approvals (HITLItem) and ready_deliverables (Deliverable) counts from the response

**workspace/serializers.py:**
- DELETE HITLItemSerializer class (research: lines 142-170)
- DELETE DeliverableSerializer class (research: lines 172-193)
- REMOVE imports: HITLItem from orchestrator.models, Deliverable from qa.models
- REMOVE `turn_count` from WorkflowRunListSerializer.fields
- REMOVE `turn_count` and `hitl_items` from WorkflowRunDetailSerializer.fields
- REMOVE OrchestratorHITLItemSerializer import if present

**workspace/urls.py:**
- REMOVE HITLItemViewSet and DeliverableViewSet router registrations
- REMOVE DeliverableHTMLView URL pattern
- REMOVE imports for removed views

**workspace/filters.py:**
- DELETE HITLItemFilter class
- DELETE DeliverableFilter class
- REMOVE imports: HITLItem from orchestrator.models, Deliverable from qa.models

**workspace/share_views.py:**
- DELETE the file entirely (ShareLink model was deleted in Plan 01). Use `rm` via bash.

**workspace/permissions.py:**
- DELETE the file entirely (IsAgencyAdmin and IsAgencyMember both removed). Use `rm` via bash.

**orchestrator/admin.py:**
- REMOVE HITLItemAdmin registration (research: lines 41-46)
- REMOVE HITLItem import

**orchestrator/executor.py:**
- Comment out dead imports and add TODO markers. Per user decision: DEFER full cleanup to Phase 4.
  ```python
  # TODO(Phase 4): Remove conflict detection -- executor rewrite from scratch
  # from hazn_platform.orchestrator.conflict_detector import detect_conflicts
  # from hazn_platform.orchestrator.conflict_detector import process_conflicts

  # TODO(Phase 4): Remove HITL imports -- executor rewrite from scratch
  # from hazn_platform.orchestrator.hitl import create_hitl_item
  # from hazn_platform.orchestrator.hitl import has_blocking_items

  # TODO(Phase 4): Remove QA imports -- executor rewrite from scratch
  # from hazn_platform.qa.runner import create_deliverable
  # from hazn_platform.qa.runner import get_task_type_for_phase
  # from hazn_platform.qa.runner import handle_qa_result
  # from hazn_platform.qa.runner import should_run_qa
  # from hazn_platform.qa.staging import deploy_to_staging
  ```
- Do NOT delete any function bodies that call these -- they will raise NameError which is acceptable (Phase 4 rewrites executor from scratch)

**orchestrator/tasks.py:**
- REMOVE `check_hitl_timeouts` task
- REMOVE `process_expired_items` import if present
- REMOVE `deliver_webhook` task (used only for HITL notifications)

**orchestrator/api/views.py:**
- REMOVE HITLItemViewSet class
- REMOVE imports: HITLItem, approve_hitl_item, reject_hitl_item, HITLItemSerializer

**orchestrator/api/urls.py:**
- REMOVE hitl router registration

**orchestrator/api/serializers.py:**
- REMOVE HITLItemSerializer class
- REMOVE HITLItem import
- REMOVE `turn_count` from WorkflowAgentSerializer.fields
- REMOVE `turn_count` and `hitl_items` from WorkflowRunSerializer.fields
- REMOVE hitl_items field declaration

**config/settings/base.py:**
- REMOVE `"hazn_platform.qa"` from LOCAL_APPS list

**DELETE source files (use bash rm):**
- hazn_platform/hazn_platform/orchestrator/conflict_detector.py
- hazn_platform/hazn_platform/orchestrator/hitl.py
- hazn_platform/hazn_platform/workspace/permissions.py (if not already handled above)
- hazn_platform/hazn_platform/workspace/share_views.py (if not already handled above)
- hazn_platform/hazn_platform/qa/ (entire directory -- `rm -rf`)

**DELETE test files (use bash rm):**
- hazn_platform/tests/test_conflict_detector.py
- hazn_platform/tests/test_hitl.py
- hazn_platform/tests/test_hitl_api.py
- hazn_platform/tests/test_qa_models.py
- hazn_platform/tests/test_qa_runner.py
- hazn_platform/tests/test_qa_approval.py
- hazn_platform/tests/test_qa_criteria.py
- hazn_platform/tests/test_qa_staging.py
- hazn_platform/tests/test_workspace_hitl.py
- hazn_platform/tests/test_workspace_deliverables.py

**UPDATE remaining test files:**
- tests/test_orchestrator_models.py: Remove any HITLItem test classes, add a basic test for WorkflowPhaseOutput.html_content and .markdown_source fields existing
- tests/test_models.py: Remove enterprise field assertions (turn_count, churned_at, deletion_* etc.), add a test for Agency singleton constraint (creating a second Agency raises ValueError)
- tests/test_workspace_clients.py: Remove agency_role from user creation fixtures, update any permission class assertions to expect IsAuthenticated
- tests/test_workspace_dashboard.py: Remove pending_approvals and ready_deliverables assertions from dashboard response checks
- tests/test_workspace_workflows.py: Remove turn_count from any serializer output assertions

**After all changes, run a comprehensive grep to verify no dangling references:**
```bash
cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform
grep -rn "HITLItem\|from.*qa\.models\|from.*qa\.runner\|IsAgencyMember\|conflict_detector\|from.*hitl import" hazn_platform/ --include="*.py" | grep -v "__pycache__" | grep -v "migrations/" | grep -v "# TODO"
```
Fix any remaining references found.
  </action>
  <verify>
    <automated>cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && grep -rn "HITLItem\|from.*qa\.models\|from.*qa\.runner\|IsAgencyMember\|from.*hitl import" hazn_platform/ --include="*.py" | grep -v "__pycache__" | grep -v "migrations/" | grep -v "# TODO" | grep -v "\.pyc" && echo "FAIL: dangling references found" || echo "PASS: no dangling references"</automated>
  </verify>
  <done>All API layer files cleaned, all dead source and test files deleted, QA app directory removed, executor.py has TODO comments, settings updated, remaining tests updated. No dangling imports of deleted models/modules in any Python file (excluding migrations and TODO comments).</done>
</task>

<task type="auto">
  <name>Task 2: Squash migrations to fresh 0001_initial per app, verify clean state</name>
  <files>
    hazn_platform/hazn_platform/orchestrator/migrations/,
    hazn_platform/hazn_platform/core/migrations/,
    hazn_platform/hazn_platform/users/migrations/,
    hazn_platform/hazn_platform/workspace/migrations/
  </files>
  <action>
This task produces the final clean migration state for v3.0. Fresh DB start means no production data to preserve.

**Step 1: Delete all existing migration files (keep __init__.py):**
```bash
cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform
find hazn_platform/*/migrations/ -name "*.py" ! -name "__init__.py" -delete
find hazn_platform/*/*/migrations/ -name "*.py" ! -name "__init__.py" -delete
```

Also delete the QA app migrations directory if it still exists (the QA app dir should be gone from Task 1, but verify):
```bash
ls hazn_platform/qa/migrations/ 2>/dev/null && rm -rf hazn_platform/qa/migrations/ || echo "QA migrations already gone"
```

**Step 2: Regenerate fresh initial migrations:**
```bash
cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform
python manage.py makemigrations
```

This should produce a clean `0001_initial.py` per app (core, orchestrator, users, workspace).

**Step 3: Verify clean state:**
```bash
python manage.py makemigrations --check
```
This MUST exit with code 0 (no new migrations needed).

**Step 4: Read the generated migration files to verify they look correct:**
- Each 0001_initial.py should reflect the final v3.0 schema
- No references to HITLItem, Deliverable (qa), conflict_detector, or enterprise fields
- WorkflowPhaseOutput should include html_content and markdown_source
- Agency should NOT have tool_preferences, churned_at, deletion_notified_at
- User should NOT have agency_role

**Step 5: Verify Django can load cleanly:**
```bash
cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform
python manage.py check --deploy 2>&1 | head -20
```
Note: --deploy may show warnings about HTTPS/HSTS in local settings -- those are fine. What matters is no model errors.

**Step 6: Run a quick Python introspection to verify complete model state:**
```bash
python -c "
import django; import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings.local')
django.setup()
from hazn_platform.orchestrator.models import WorkflowPhaseOutput, WorkflowRun
from hazn_platform.core.models import Agency, EndClient
from hazn_platform.users.models import User
print('All models import cleanly')
print('PhaseOutput fields:', [f.name for f in WorkflowPhaseOutput._meta.get_fields()])
print('Agency fields:', [f.name for f in Agency._meta.get_fields()])
print('User fields:', [f.name for f in User._meta.get_fields()])
"
```
  </action>
  <verify>
    <automated>cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && python manage.py makemigrations --check && echo "PASS: no pending migrations" || echo "FAIL: pending migrations exist"</automated>
  </verify>
  <done>All app migrations squashed to fresh 0001_initial.py. `python manage.py makemigrations --check` exits cleanly with code 0. No references to deleted models in migration files. Django check passes without model errors.</done>
</task>

</tasks>

<verification>
1. `grep -rn "HITLItem\|from.*qa\.\|IsAgencyMember\|conflict_detector" hazn_platform/ --include="*.py" | grep -v __pycache__ | grep -v migrations/ | grep -v "# TODO"` returns empty
2. `python manage.py makemigrations --check` exits 0
3. `python manage.py check` passes without model errors
4. `ls hazn_platform/qa/` returns "No such file or directory"
5. `ls hazn_platform/orchestrator/hitl.py hazn_platform/orchestrator/conflict_detector.py hazn_platform/workspace/permissions.py 2>&1` returns file not found for all three
6. All 10 dead test files are deleted
7. executor.py contains TODO(Phase 4) comments for dead imports
</verification>

<success_criteria>
- Zero dangling Python imports of deleted models/modules (excluding TODO comments and migration files)
- QA app directory fully deleted, removed from INSTALLED_APPS
- conflict_detector.py, hitl.py, permissions.py deleted
- All 10 dead test files deleted
- Remaining test files updated (no references to deleted fields/models)
- IsAuthenticated replaces IsAgencyMember in all viewsets
- executor.py dead imports commented with TODO(Phase 4) markers
- Fresh 0001_initial.py per app reflecting final v3.0 schema
- `python manage.py makemigrations --check` exits cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-simplification/02-02-SUMMARY.md`
</output>
