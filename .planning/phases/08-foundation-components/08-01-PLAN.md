---
phase: 08-foundation-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hazn_platform/hazn_platform/testing/__init__.py
  - hazn_platform/hazn_platform/testing/mocks.py
  - hazn_platform/hazn_platform/testing/fixtures.py
  - hazn_platform/tests/fixtures/tool_use_responses.json
  - hazn_platform/tests/fixtures/agent_sdk_tool_calls.json
  - hazn_platform/tests/fixtures/agent_outputs/seo_audit_report.md
  - hazn_platform/tests/fixtures/agent_outputs/analytics_findings.md
  - hazn_platform/tests/fixtures/agent_outputs/code_generation.md
  - hazn_platform/hazn_platform/orchestrator/prompt_assembler.py
  - hazn_platform/tests/test_prompt_assembler.py
autonomous: true
requirements:
  - RUNT-01

must_haves:
  truths:
    - "Given an agent name, skill list, and client context, the system produces a complete system prompt containing agent persona, skill instructions, and L2/L3 client data"
    - "System prompt includes full SKILL.md content for each declared skill (not truncated)"
    - "Small reference docs are inlined in the prompt; large reference docs get a size note instead"
    - "A reusable mock module exists for Phase 9+ to import mock LLM responses and mock MCP dispatch"
  artifacts:
    - path: "hazn_platform/hazn_platform/orchestrator/prompt_assembler.py"
      provides: "PromptAssembler with assemble_prompt() and _read_skill_content()"
      exports: ["assemble_prompt", "_read_skill_content"]
    - path: "hazn_platform/hazn_platform/testing/mocks.py"
      provides: "Reusable mock builders for LLM responses and MCP dispatch"
      exports: ["MockLLMResponse", "MockToolDispatcher"]
    - path: "hazn_platform/hazn_platform/testing/fixtures.py"
      provides: "Fixture loaders for test data files"
      exports: ["load_fixture_json", "load_fixture_text"]
    - path: "hazn_platform/tests/test_prompt_assembler.py"
      provides: "Tests for RUNT-01 prompt assembly"
      min_lines: 80
  key_links:
    - from: "hazn_platform/hazn_platform/orchestrator/prompt_assembler.py"
      to: "hazn_platform/hazn_platform/orchestrator/agent_manager.py"
      via: "import read_agent_persona"
      pattern: "from hazn_platform\\.orchestrator\\.agent_manager import read_agent_persona"
    - from: "hazn_platform/hazn_platform/orchestrator/prompt_assembler.py"
      to: "hazn/skills/*/SKILL.md"
      via: "pathlib file read"
      pattern: "skill_path.*read_text"
---

<objective>
Create the reusable testing infrastructure (mock module + fixture files) and implement PromptAssembler -- the component that constructs complete system prompts from agent persona markdown, skill SKILL.md content, and L2/L3 client context.

Purpose: Establishes the shared test foundation that Plans 02 and 03 depend on, and delivers the first of three foundation components (RUNT-01). PromptAssembler is the entry point for every agent execution -- it runs once per phase to produce the system prompt before any LLM call.

Output: `hazn_platform/testing/` mock module, `tests/fixtures/` with JSON and markdown fixtures, `prompt_assembler.py` with full test coverage.
</objective>

<execution_context>
@/Users/rizwanqaiser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rizwanqaiser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-foundation-components/08-CONTEXT.md
@.planning/phases/08-foundation-components/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From hazn_platform/orchestrator/agent_manager.py:
```python
def read_agent_persona(
    agent_type: str,
    agents_dir: Path | None = None,
) -> str:
    """Read agent persona markdown from hazn/agents/{agent_type}.md."""
```

From hazn_platform/orchestrator/workflow_models.py:
```python
class WorkflowPhaseSchema(BaseModel):
    model_config = ConfigDict(extra="allow")
    id: str
    name: str
    agent: str | None = None
    skills: list[str] = Field(default_factory=list)
    tools: list[str] = Field(default_factory=list)
    # ... other fields
```

From hazn_platform/core/memory_types.py:
```python
class ClientContext(BaseModel):
    agency: dict
    client: dict
    brand_voice: Optional[str] = None
    active_campaigns: list[dict]
    top_keywords: list[dict]
```

Default agents directory (from agent_manager.py line 24):
```python
_DEFAULT_AGENTS_DIR = Path(__file__).resolve().parent.parent.parent / "hazn" / "agents"
```

Skills directory structure:
```
hazn/skills/{skill_name}/SKILL.md          # Main skill document
hazn/skills/{skill_name}/references/*.md   # Optional reference docs
```

SKILL.md frontmatter format (from seo-audit):
```yaml
---
name: seo-audit
description: Run a comprehensive SEO audit on any external website.
allowed-tools: web_fetch, web_search, Bash, Read, Write
---
```

Pytest config (from pyproject.toml):
```
addopts = "--ds=config.settings.test --reuse-db --import-mode=importlib"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create testing infrastructure -- mock module and fixture files</name>
  <files>
    hazn_platform/hazn_platform/testing/__init__.py,
    hazn_platform/hazn_platform/testing/mocks.py,
    hazn_platform/hazn_platform/testing/fixtures.py,
    hazn_platform/tests/fixtures/tool_use_responses.json,
    hazn_platform/tests/fixtures/agent_sdk_tool_calls.json,
    hazn_platform/tests/fixtures/agent_outputs/seo_audit_report.md,
    hazn_platform/tests/fixtures/agent_outputs/analytics_findings.md,
    hazn_platform/tests/fixtures/agent_outputs/code_generation.md
  </files>
  <behavior>
    - load_fixture_json("tool_use_responses.json") returns a list of dicts with "type": "tool_use", "id", "name", "input" keys
    - load_fixture_json("agent_sdk_tool_calls.json") returns a list of dicts with "tool_name", "tool_input" keys
    - load_fixture_text("agent_outputs/seo_audit_report.md") returns non-empty string containing "## Findings" and "## Recommendations"
    - MockLLMResponse can build a mock assistant message with tool_use content blocks
    - MockToolDispatcher can register a tool and return a canned result
  </behavior>
  <action>
    1. Create `hazn_platform/hazn_platform/testing/__init__.py` with re-exports from mocks and fixtures modules.

    2. Create `hazn_platform/hazn_platform/testing/mocks.py`:
       - `MockLLMResponse` class: builds mock Anthropic API assistant messages. Constructor takes `content_blocks: list[dict]`. Helper methods: `with_text(text)`, `with_tool_use(id, name, input_dict)`. Returns the full message dict shape: `{"role": "assistant", "content": [...]}`.
       - `MockToolDispatcher` class: wraps a dict of tool_name -> canned_result. Methods: `register(name, result)`, `dispatch(tool_use_block) -> tool_result_block`. Returns Anthropic-native tool_result format.

    3. Create `hazn_platform/hazn_platform/testing/fixtures.py`:
       - `FIXTURES_DIR = Path(__file__).resolve().parent.parent.parent / "tests" / "fixtures"` (points to hazn_platform/tests/fixtures/).
       - `load_fixture_json(relative_path: str) -> Any`: loads and parses JSON fixture file.
       - `load_fixture_text(relative_path: str) -> str`: loads fixture file as text.

    4. Create `hazn_platform/tests/fixtures/tool_use_responses.json`:
       Array of 3 realistic Anthropic API tool_use content blocks. Include:
       - `load_context` tool call with agent_id, l3_client_id, l2_agency_id params
       - `search_archival` tool call with query and agent_id params
       - `get_analytics_data` tool call with domain and date_range params
       Use realistic tool_use IDs (format: "toolu_" followed by 24 alphanumeric chars).

    5. Create `hazn_platform/tests/fixtures/agent_sdk_tool_calls.json`:
       Array of 2 Claude Agent SDK format tool calls:
       - Each entry: `{"tool_name": "...", "tool_input": {...}}` (SDK passes name and input separately)
       - Include `load_context` and `search_archival` tool calls

    6. Create `hazn_platform/tests/fixtures/agent_outputs/seo_audit_report.md`:
       A realistic ~800-word SEO audit report following the convention-based format agents are instructed to use. Must include:
       - `# SEO Audit Report: example.com` title
       - `## Score Summary` section with overall score
       - `## Findings` section with 3-4 findings, each as a bullet list with **Severity**, **Description**, **Evidence**, **Recommendation** fields
       - `## Recommendations` section with prioritized action items
       - `## Metadata` section with audit date, pages crawled, tools used

    7. Create `hazn_platform/tests/fixtures/agent_outputs/analytics_findings.md`:
       A realistic analytics audit output with:
       - `## Findings` section with 3 findings using the same structured format
       - Different content focus (analytics, tracking, data quality)

    8. Create `hazn_platform/tests/fixtures/agent_outputs/code_generation.md`:
       A realistic agent output containing code artifacts. Must include:
       - A regular markdown section with explanation text
       - Two ` ```artifact javascript src/tracking/gtm-setup.js ` code blocks (using the convention tag that OutputCollector will parse)
       - One regular code block without artifact tag (should NOT be extracted as code artifact)
  </action>
  <verify>
    <automated>cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && python -c "from hazn_platform.testing.fixtures import load_fixture_json, load_fixture_text; data = load_fixture_json('tool_use_responses.json'); assert len(data) >= 3; text = load_fixture_text('agent_outputs/seo_audit_report.md'); assert '## Findings' in text; print('Fixtures OK')"</automated>
  </verify>
  <done>
    - hazn_platform/testing/ module exists with __init__.py, mocks.py, fixtures.py
    - tests/fixtures/ contains tool_use_responses.json, agent_sdk_tool_calls.json, and 3 agent output markdown files
    - MockLLMResponse builds well-formed Anthropic API response dicts
    - MockToolDispatcher registers tools and returns tool_result format dicts
    - load_fixture_json and load_fixture_text load and return fixture data correctly
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PromptAssembler with TDD (RUNT-01)</name>
  <files>
    hazn_platform/hazn_platform/orchestrator/prompt_assembler.py,
    hazn_platform/tests/test_prompt_assembler.py
  </files>
  <behavior>
    - assemble_prompt("strategist", [], None) returns string containing strategist persona markdown
    - assemble_prompt("seo-specialist", ["seo-audit"], None) returns string containing both persona AND full SKILL.md content for seo-audit (not truncated)
    - assemble_prompt("strategist", ["seo-audit"], {"agency": {...}, "client": {...}}) returns string with persona + skill + client context sections
    - assemble_prompt("nonexistent-agent", [], None) raises ValueError
    - _read_skill_content("seo-audit") returns full SKILL.md text
    - _read_skill_content("nonexistent-skill") raises ValueError
    - Reference docs under reference_size_limit (default 15KB) are inlined with "## Reference: {name}" header
    - Reference docs over reference_size_limit get a size note instead of content
    - Skill frontmatter allowed-tools field is logged but NOT used for tool scoping (workflow YAML is authoritative)
    - Prompt sections appear in order: persona, skills, client context
  </behavior>
  <action>
    RED phase first -- write test_prompt_assembler.py before implementation.

    1. Create `hazn_platform/tests/test_prompt_assembler.py`:
       - Use real agent persona files and real SKILL.md files from `hazn/agents/` and `hazn/skills/` (following the locked decision for "real content with mock interfaces").
       - Define path constants at top: `_HOST_AGENTS_DIR`, `_HOST_SKILLS_DIR` using the same pattern as test_workflow_parser.py (Path relative to test file, with fallback).
       - `HAS_REAL_CONTENT` flag: True if agents dir has .md files and skills dir has SKILL.md files.

       Test classes:
       - `TestAssemblePromptBasic`: Tests with real agent personas.
         - test_persona_only: assemble_prompt("strategist", []) returns persona content
         - test_unknown_agent_raises: assemble_prompt("nonexistent") raises ValueError
         - test_returns_string: result is a str, not None

       - `TestSkillInjection` (skip if not HAS_REAL_CONTENT):
         - test_single_skill_included: assemble_prompt("seo-specialist", ["seo-audit"]) contains the seo-audit SKILL.md content
         - test_skill_content_not_truncated: full SKILL.md is present (check for a string that only appears later in the file)
         - test_multiple_skills: assemble_prompt with 2 skills contains both
         - test_unknown_skill_raises: skill "nonexistent-skill-xyz" raises ValueError
         - test_skill_section_header: output contains "# Skill: seo-audit"

       - `TestReferenceHandling`:
         - Create temp skill dir with SKILL.md + references/ containing small and large .md files
         - test_small_reference_inlined: reference under 15KB appears in output with "## Reference:" header
         - test_large_reference_noted: reference over 15KB gets size note, not content
         - test_no_references_dir: skill without references/ dir works fine

       - `TestClientContextInjection`:
         - test_context_section_present: assemble_prompt with client_context dict includes "# Client Context"
         - test_context_contains_agency_data: agency info appears in output
         - test_no_context_section_when_none: assemble_prompt with client_context=None has no "# Client Context"

       - `TestPromptOrdering`:
         - test_persona_before_skills: persona text appears at lower index than skill text
         - test_skills_before_context: skill text appears before client context text

    2. Run tests -- all should FAIL (RED).

    3. Create `hazn_platform/hazn_platform/orchestrator/prompt_assembler.py`:
       Follow the design from 08-RESEARCH.md Pattern 1. Key implementation details:

       - `_read_skill_content(skill_name, skills_dir, include_references, reference_size_limit)`:
         - Default `skills_dir` = `Path(__file__).resolve().parent.parent.parent / "hazn" / "skills"`
         - Read `{skills_dir}/{skill_name}/SKILL.md`
         - If `include_references` and `references/` subdir exists, iterate sorted .md files:
           - Under `reference_size_limit` (default 15000): inline with `## Reference: {stem}` header
           - Over limit: append note with file size
         - Log skill frontmatter `allowed-tools` at INFO level but DO NOT enforce (per user decision: workflow YAML tools: field is authoritative)

       - `assemble_prompt(agent_type, skills, client_context, agents_dir, skills_dir)`:
         - Section 1: Agent persona via `read_agent_persona(agent_type, agents_dir=agents_dir)`
         - Section 2: For each skill in skills list, call `_read_skill_content()` and prepend with `# Skill: {skill_name}` header separated by `---`
         - Section 3: If `client_context` is not None, format as `# Client Context` section
         - `_format_client_context(context: dict) -> str`: format client context dict as readable markdown. Iterate top-level keys, render nested dicts/lists as indented items.
         - Join all sections with `\n\n`
         - Log assembled prompt stats: agent_type, skill count, context present/absent, total character count

    4. Run tests -- all should PASS (GREEN).

    Do NOT import from `hazn_platform.core.letta_client` or any other module that requires Django setup / running services. The prompt_assembler module must be importable without Django configured (it only uses `read_agent_persona` which is a pure file-read function, and pathlib for skill files).

    IMPORTANT: `read_agent_persona` imports from `hazn_platform.core.letta_client` at module top level in agent_manager.py. To avoid triggering Letta/Django setup during import, the prompt_assembler should NOT import from agent_manager.py directly. Instead, duplicate the persona file-reading logic (it's 5 lines: resolve path, check exists, read_text) to keep prompt_assembler self-contained and testable without Django. Add a comment noting the duplication is intentional to avoid the Letta import chain.
  </action>
  <verify>
    <automated>cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && .venv/bin/pytest tests/test_prompt_assembler.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - prompt_assembler.py exists with assemble_prompt() and _read_skill_content() functions
    - All test_prompt_assembler.py tests pass
    - Prompt includes full SKILL.md content (not truncated) per user decision
    - Small references inlined, large references noted with size
    - Client context formatted and included when provided
    - Module is importable without Django setup (no transitive Letta/Django imports)
    - No LLM calls, no API keys, no running services needed
  </done>
</task>

</tasks>

<verification>
```bash
# All Phase 8 Plan 01 tests pass
cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && .venv/bin/pytest tests/test_prompt_assembler.py -x -q

# Mock module is importable
cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && python -c "from hazn_platform.testing import MockLLMResponse, MockToolDispatcher, load_fixture_json, load_fixture_text; print('Mock module OK')"

# Fixtures load correctly
cd /Users/rizwanqaiser/Work/autonomous/hazn/hazn_platform && python -c "from hazn_platform.testing.fixtures import load_fixture_json; data = load_fixture_json('tool_use_responses.json'); assert all(d['type'] == 'tool_use' for d in data); print(f'{len(data)} tool_use fixtures loaded')"
```
</verification>

<success_criteria>
- PromptAssembler produces complete system prompts with persona + skills + context
- Full SKILL.md content injected (not truncated) per locked decision
- Reference docs handled by size (inline small, note large) per Claude's discretion
- Mock module and fixtures usable by Plans 02 and 03
- All tests pass without Django setup, LLM calls, or running services
</success_criteria>

<output>
After completion, create `.planning/phases/08-foundation-components/08-01-SUMMARY.md`
</output>
