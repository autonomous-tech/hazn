---
phase: 03-tool-migration
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - hazn_platform/hazn_platform/orchestrator/apps.py
  - hazn_platform/hazn_platform/orchestrator/backends/agent_sdk.py
  - hazn_platform/hazn_platform/orchestrator/agent_runner.py
  - hazn_platform/hazn_platform/orchestrator/executor.py
  - hazn_platform/hazn_platform/mcp_servers/hazn_memory_server.py
  - hazn_platform/hazn_platform/mcp_servers/analytics_server.py
  - hazn_platform/hazn_platform/mcp_servers/github_server.py
  - hazn_platform/hazn_platform/mcp_servers/vercel_server.py
  - hazn_platform/hazn_platform/mcp_servers/__init__.py
  - hazn_platform/hazn_platform/orchestrator/tool_router.py
  - hazn_platform/hazn_platform/orchestrator/tool_wiring.py
  - hazn_platform/hazn_platform/orchestrator/data_tools.py
  - hazn_platform/pyproject.toml
  - hazn_platform/tests/test_tool_router.py
  - hazn_platform/tests/test_tool_wiring.py
  - hazn_platform/tests/test_data_tools.py
  - hazn_platform/tests/test_mcp_analytics_server.py
  - hazn_platform/tests/test_mcp_github_server.py
  - hazn_platform/tests/test_mcp_memory_server.py
  - hazn_platform/tests/test_mcp_vercel_server.py
  - hazn_platform/tests/test_agent_sdk_backend.py
  - hazn_platform/tests/test_output_collector.py
  - hazn_platform/tests/test_agent_runner.py
autonomous: true
requirements: [STRP-01, TOOL-08]

must_haves:
  truths:
    - "All 4 MCP server files are deleted and mcp_servers/ directory is gone"
    - "tool_router.py, tool_wiring.py, and data_tools.py are deleted"
    - "fastmcp is removed from pyproject.toml dependencies"
    - "apps.py OrchestratorConfig.ready() uses ToolRegistry instead of ToolRouter"
    - "AgentSDKBackend uses ToolRegistry instead of ToolRouter"
    - "No import of hazn_platform.mcp_servers or hazn_platform.orchestrator.tool_router anywhere in codebase"
    - "Old test files for MCP servers and tool_router/wiring are deleted"
    - "Django starts without errors after all deletions"
  artifacts:
    - path: "hazn_platform/hazn_platform/orchestrator/apps.py"
      provides: "OrchestratorConfig.ready() wiring ToolRegistry at startup"
      contains: "from hazn_platform.orchestrator.tools"
    - path: "hazn_platform/hazn_platform/orchestrator/backends/agent_sdk.py"
      provides: "AgentSDKBackend using ToolRegistry.get_server() and get_allowed_tools()"
      contains: "ToolRegistry"
  key_links:
    - from: "hazn_platform/hazn_platform/orchestrator/apps.py"
      to: "hazn_platform/hazn_platform/orchestrator/tools/__init__.py"
      via: "build_registry() called in ready()"
      pattern: "build_registry"
    - from: "hazn_platform/hazn_platform/orchestrator/backends/agent_sdk.py"
      to: "hazn_platform/hazn_platform/orchestrator/tools/registry.py"
      via: "ToolRegistry instance for get_server() and get_allowed_tools()"
      pattern: "ToolRegistry"
---

<objective>
Wire ToolRegistry into the executor chain, delete all MCP servers and old routing infrastructure, remove fastmcp dependency.

Purpose: Completes the migration by connecting the new tool system to the existing executor infrastructure and removing all MCP/FastMCP code. After this plan, no MCP protocol code remains in the codebase.

Output: Clean codebase with ToolRegistry as the sole tool system, all MCP servers and routing code deleted.
</objective>

<execution_context>
@/Users/rizwanqaiser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rizwanqaiser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tool-migration/03-CONTEXT.md
@.planning/phases/03-tool-migration/03-RESEARCH.md
@.planning/phases/03-tool-migration/03-01-SUMMARY.md
@.planning/phases/03-tool-migration/03-02-SUMMARY.md
@.planning/phases/03-tool-migration/03-03-SUMMARY.md

<interfaces>
<!-- Files that import from modules being deleted -->

From orchestrator/apps.py (MUST UPDATE):
```python
def build_tool_registry():
    from hazn_platform.orchestrator.tool_router import build_tool_registry as _build
    return _build()

def wire_callables(router):
    from hazn_platform.orchestrator.tool_wiring import wire_callables as _wire
    return _wire(router)

def validate_registry(router):
    from hazn_platform.orchestrator.tool_router import validate_registry as _validate
    return _validate(router)

class OrchestratorConfig(AppConfig):
    def ready(self):
        import hazn_platform.orchestrator.tool_wiring as tool_wiring_module
        router = build_tool_registry()
        count = wire_callables(router)
        tool_wiring_module._ROUTER_SINGLETON = router
        discrepancies = validate_registry(router)
```

From orchestrator/backends/agent_sdk.py (MUST UPDATE):
```python
from hazn_platform.orchestrator.tool_router import ToolRouter

class AgentSDKBackend:
    def __init__(self, router: ToolRouter) -> None:
        self._router = router
    def _build_allowed_tools(self, tools: list[dict]) -> list[str]:
        return [f"mcp__hazn__{tool['name']}" for tool in tools]
```

From orchestrator/agent_runner.py (MUST UPDATE):
```python
from hazn_platform.orchestrator.tool_router import ToolRouter
from hazn_platform.orchestrator.tool_wiring import scope_tools_for_phase
```

From orchestrator/executor.py (MUST UPDATE):
```python
from hazn_platform.orchestrator.tool_router import build_tool_registry
# References tool_wiring._ROUTER_SINGLETON
```

Test files that import deleted modules (ALL MUST BE DELETED):
- tests/test_tool_router.py
- tests/test_tool_wiring.py
- tests/test_data_tools.py
- tests/test_mcp_analytics_server.py
- tests/test_mcp_github_server.py
- tests/test_mcp_memory_server.py
- tests/test_mcp_vercel_server.py

Test files that import ToolRouter (MUST UPDATE):
- tests/test_agent_sdk_backend.py -- imports ToolRegistryEntry, ToolRouter
- tests/test_output_collector.py -- imports ToolRouter, ToolRegistryEntry
- tests/test_agent_runner.py -- imports ToolRegistryEntry, ToolRouter

From orchestrator/tools/ (new system, created in Plans 01-03):
```python
from hazn_platform.orchestrator.tools import ToolRegistry, build_registry
registry = build_registry()
server = registry.get_server()
allowed = registry.get_allowed_tools(phase_tools=["tool_a", "tool_b"])
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ToolRegistry into apps.py, agent_sdk.py, agent_runner.py, executor.py</name>
  <files>
    hazn_platform/hazn_platform/orchestrator/apps.py,
    hazn_platform/hazn_platform/orchestrator/backends/agent_sdk.py,
    hazn_platform/hazn_platform/orchestrator/agent_runner.py,
    hazn_platform/hazn_platform/orchestrator/executor.py,
    hazn_platform/tests/test_agent_sdk_backend.py,
    hazn_platform/tests/test_output_collector.py,
    hazn_platform/tests/test_agent_runner.py
  </files>
  <action>
    Update `apps.py` OrchestratorConfig:
    - Replace the 3 lazy import helpers (build_tool_registry, wire_callables, validate_registry) with a single one that imports `build_registry` from `hazn_platform.orchestrator.tools`
    - In `ready()`: call `build_registry()` to get a fully-wired ToolRegistry. Store it as a module-level singleton (replace `_ROUTER_SINGLETON` pattern). No separate wire/validate steps needed -- build_registry() creates the registry with all tools pre-registered.
    - Remove all references to tool_router and tool_wiring modules

    Update `backends/agent_sdk.py`:
    - Replace `from hazn_platform.orchestrator.tool_router import ToolRouter` with `from hazn_platform.orchestrator.tools import ToolRegistry`
    - Change `__init__(self, router: ToolRouter)` to `__init__(self, registry: ToolRegistry)`
    - Replace `_build_allowed_tools` to use `registry.get_allowed_tools(phase_tools)` directly (the ToolRegistry already handles mcp__hazn__ prefixing)
    - In `execute()`: pass `registry.get_server()` as the MCP server config to ClaudeAgentOptions (or prepare for Phase 4 executor rewrite with a TODO comment if the full SDK wiring happens there)

    Update `agent_runner.py`:
    - Replace `from hazn_platform.orchestrator.tool_router import ToolRouter` with `from hazn_platform.orchestrator.tools import ToolRegistry`
    - Replace `from hazn_platform.orchestrator.tool_wiring import scope_tools_for_phase` -- the ToolRegistry.get_allowed_tools() replaces this
    - Update any references to use ToolRegistry instead of ToolRouter

    Update `executor.py`:
    - Replace `from hazn_platform.orchestrator.tool_router import build_tool_registry` with `from hazn_platform.orchestrator.tools import build_registry`
    - Replace any reference to `tool_wiring._ROUTER_SINGLETON` with the new singleton from apps.py

    Update test files that import ToolRouter:
    - `tests/test_agent_sdk_backend.py`: Replace ToolRouter/ToolRegistryEntry imports with ToolRegistry; update test setup to use new registry
    - `tests/test_output_collector.py`: Replace ToolRouter/ToolRegistryEntry imports with ToolRegistry
    - `tests/test_agent_runner.py`: Replace ToolRouter/ToolRegistryEntry imports with ToolRegistry

    After all updates, verify no imports of tool_router or tool_wiring remain in source files (test files for these modules will be deleted in Task 2).
  </action>
  <verify>
    <automated>cd hazn_platform && DJANGO_SETTINGS_MODULE=config.settings.test DATABASE_URL=sqlite:///test.db python -c "import django; django.setup(); print('Django starts OK')" && python -m pytest tests/test_tool_registry.py tests/test_agent_sdk_backend.py -x -q 2>/dev/null || echo "Some tests may need SDK installed"</automated>
  </verify>
  <done>
    apps.py uses build_registry() from tools package. agent_sdk.py uses ToolRegistry. agent_runner.py uses ToolRegistry. executor.py uses build_registry(). No source file imports tool_router or tool_wiring. Django starts without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete MCP servers, old routing, old tests, remove fastmcp</name>
  <files>
    hazn_platform/hazn_platform/mcp_servers/hazn_memory_server.py,
    hazn_platform/hazn_platform/mcp_servers/analytics_server.py,
    hazn_platform/hazn_platform/mcp_servers/github_server.py,
    hazn_platform/hazn_platform/mcp_servers/vercel_server.py,
    hazn_platform/hazn_platform/mcp_servers/__init__.py,
    hazn_platform/hazn_platform/orchestrator/tool_router.py,
    hazn_platform/hazn_platform/orchestrator/tool_wiring.py,
    hazn_platform/hazn_platform/orchestrator/data_tools.py,
    hazn_platform/tests/test_tool_router.py,
    hazn_platform/tests/test_tool_wiring.py,
    hazn_platform/tests/test_data_tools.py,
    hazn_platform/tests/test_mcp_analytics_server.py,
    hazn_platform/tests/test_mcp_github_server.py,
    hazn_platform/tests/test_mcp_memory_server.py,
    hazn_platform/tests/test_mcp_vercel_server.py,
    hazn_platform/pyproject.toml
  </files>
  <action>
    Delete ALL MCP server files:
    - `hazn_platform/hazn_platform/mcp_servers/hazn_memory_server.py`
    - `hazn_platform/hazn_platform/mcp_servers/analytics_server.py`
    - `hazn_platform/hazn_platform/mcp_servers/github_server.py`
    - `hazn_platform/hazn_platform/mcp_servers/vercel_server.py`
    - `hazn_platform/hazn_platform/mcp_servers/__init__.py`
    - Remove `hazn_platform/hazn_platform/mcp_servers/` directory entirely (including __pycache__)

    Delete old routing files:
    - `hazn_platform/hazn_platform/orchestrator/tool_router.py`
    - `hazn_platform/hazn_platform/orchestrator/tool_wiring.py`
    - `hazn_platform/hazn_platform/orchestrator/data_tools.py`

    Delete old test files:
    - `hazn_platform/tests/test_tool_router.py`
    - `hazn_platform/tests/test_tool_wiring.py`
    - `hazn_platform/tests/test_data_tools.py`
    - `hazn_platform/tests/test_mcp_analytics_server.py`
    - `hazn_platform/tests/test_mcp_github_server.py`
    - `hazn_platform/tests/test_mcp_memory_server.py`
    - `hazn_platform/tests/test_mcp_vercel_server.py`

    Update `pyproject.toml`:
    - Remove `"fastmcp>=3.1.0"` from dependencies
    - Add `"beautifulsoup4>=4.12.0"` and `"aiofiles>=23.0"` to dependencies (needed by new filesystem.py and web.py tools)
    - Add `"claude-agent-sdk>=0.1.47"` to dependencies (needed by ToolRegistry, was previously lazy-imported from claude_code_sdk)

    After all deletions, verify:
    1. `grep -r "from hazn_platform.mcp_servers" hazn_platform/` returns nothing (excluding .venv)
    2. `grep -r "from hazn_platform.orchestrator.tool_router" hazn_platform/` returns nothing
    3. `grep -r "from hazn_platform.orchestrator.tool_wiring" hazn_platform/` returns nothing
    4. `grep -r "from hazn_platform.orchestrator.data_tools" hazn_platform/` returns nothing
    5. `grep -r "fastmcp" hazn_platform/` returns nothing (excluding .venv)
    6. Django starts cleanly: `DJANGO_SETTINGS_MODULE=config.settings.test DATABASE_URL=sqlite:///test.db python -c "import django; django.setup()"`
  </action>
  <verify>
    <automated>cd hazn_platform && grep -r "from hazn_platform.mcp_servers" hazn_platform/ --include="*.py" | grep -v ".venv" | grep -v __pycache__ && echo "FAIL: MCP imports still exist" || echo "PASS: No MCP imports" && grep -r "fastmcp" hazn_platform/ --include="*.py" --include="*.toml" | grep -v ".venv" | grep -v __pycache__ && echo "FAIL: fastmcp still referenced" || echo "PASS: No fastmcp references" && DJANGO_SETTINGS_MODULE=config.settings.test DATABASE_URL=sqlite:///test.db python -c "import django; django.setup(); print('Django starts OK')"</automated>
  </verify>
  <done>
    mcp_servers/ directory deleted. tool_router.py, tool_wiring.py, data_tools.py deleted. 7 old test files deleted. fastmcp removed from pyproject.toml. beautifulsoup4 + aiofiles + claude-agent-sdk added. No dangling imports. Django starts cleanly.
  </done>
</task>

</tasks>

<verification>
cd hazn_platform && python -m pytest tests/test_tool_registry.py tests/test_tools_filesystem.py tests/test_tools_web.py tests/test_tools_memory.py tests/test_tools_github.py tests/test_tools_vercel.py tests/test_tools_analytics.py -x -q
</verification>

<success_criteria>
- mcp_servers/ directory completely gone
- tool_router.py, tool_wiring.py, data_tools.py completely gone
- fastmcp removed from dependencies
- claude-agent-sdk, beautifulsoup4, aiofiles in dependencies
- apps.py uses build_registry() from orchestrator/tools
- agent_sdk.py uses ToolRegistry
- No import of deleted modules anywhere in codebase
- Django starts without errors
- All new tool tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-tool-migration/03-04-SUMMARY.md`
</output>
