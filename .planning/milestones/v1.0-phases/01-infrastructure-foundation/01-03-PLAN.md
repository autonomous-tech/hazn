---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - hazn_platform/core/vault.py
  - hazn_platform/core/letta_client.py
  - hazn_platform/core/management/commands/validate_letta.py
  - hazn_platform/core/management/commands/seed_dev_data.py
  - tests/test_vault.py
  - tests/test_letta.py
  - scripts/validate_services.sh
autonomous: true
requirements:
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "Vault stores and retrieves a test secret via hvac Python client"
    - "Postgres VaultCredential table stores only vault_secret_id (path string), never the raw secret"
    - "Letta server accepts agent creation via letta-client Python SDK"
    - "Letta server accepts archival memory insert and semantic search"
    - "Seed data command creates test agency, 2-3 end-clients, brand voice, keywords, and a test Vault secret"
  artifacts:
    - path: "hazn_platform/core/vault.py"
      provides: "Vault client helpers (get_vault_client, store_secret, read_secret)"
      exports: ["get_vault_client", "store_secret", "read_secret"]
    - path: "hazn_platform/core/letta_client.py"
      provides: "Letta client factory (get_letta_client)"
      exports: ["get_letta_client"]
    - path: "hazn_platform/core/management/commands/seed_dev_data.py"
      provides: "Django management command for dev seed data"
      contains: "seed_dev_data"
    - path: "hazn_platform/core/management/commands/validate_letta.py"
      provides: "Django management command to validate Letta connectivity"
      contains: "validate_letta"
    - path: "tests/test_vault.py"
      provides: "Integration tests for Vault store/retrieve"
      min_lines: 30
    - path: "tests/test_letta.py"
      provides: "Integration tests for Letta agent creation and archival ops"
      min_lines: 40
    - path: "scripts/validate_services.sh"
      provides: "Smoke test script checking all service health"
      contains: "docker compose"
  key_links:
    - from: "hazn_platform/core/vault.py"
      to: "HashiCorp Vault"
      via: "hvac client with VAULT_ADDR and VAULT_TOKEN from Django settings"
      pattern: "hvac\\.Client"
    - from: "hazn_platform/core/letta_client.py"
      to: "Letta server"
      via: "letta-client SDK with LETTA_BASE_URL and LETTA_SERVER_PASSWORD"
      pattern: "Letta\\("
    - from: "hazn_platform/core/management/commands/seed_dev_data.py"
      to: "hazn_platform/core/vault.py"
      via: "store_secret() call to create test Vault secret"
      pattern: "store_secret"
    - from: "hazn_platform/core/models.py (VaultCredential)"
      to: "hazn_platform/core/vault.py"
      via: "vault_secret_id field stores Vault path, read_secret() retrieves"
      pattern: "vault_secret_id"
---

<objective>
Implement Vault and Letta integration modules, validate both services work end-to-end with Django, create seed data command, and write integration tests proving all Phase 1 success criteria are met.

Purpose: This plan proves the infrastructure actually works together. Plan 01 stood up the services, Plan 02 created the schema -- this plan wires Django to Vault and Letta, seeds realistic test data, and validates every Phase 1 success criterion.

Output: Working Vault and Letta clients, seed data command, integration tests, and service validation script.
</objective>

<execution_context>
@/Users/rizwanqaiser/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rizwanqaiser/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-foundation/01-VALIDATION.md
@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- Django settings available -->
VAULT_ADDR: str  # http://vault:8200 (Docker internal) or http://localhost:8200 (host)
VAULT_TOKEN: str  # Root token from .vault-keys
LETTA_BASE_URL: str  # http://letta:8283 (Docker internal) or http://localhost:8283 (host)
LETTA_SERVER_PASSWORD: str  # "letta" (default)
LANGFUSE_PUBLIC_KEY: str
LANGFUSE_SECRET_KEY: str
LANGFUSE_HOST: str

<!-- From Plan 02 -- Django models available -->
from hazn_platform.core.models import Agency, EndClient, VaultCredential
from hazn_platform.marketing.models import Keyword, Audit, Campaign, Decision
from hazn_platform.content.models import BrandVoice, ApprovedCopy

<!-- Vault API pattern (from research) -->
hvac.Client(url=settings.VAULT_ADDR, token=settings.VAULT_TOKEN)
client.secrets.kv.v2.create_or_update_secret(path=path, secret=data, mount_point="secret")
client.secrets.kv.v2.read_secret_version(path=path, mount_point="secret")

<!-- Letta SDK pattern (from research) -->
from letta_client import Letta
client = Letta(base_url=settings.LETTA_BASE_URL, api_key=settings.LETTA_SERVER_PASSWORD)
client.agents.create(name=..., model=..., memory_blocks=[...])
client.agents.passages.insert(agent_id=..., content=..., tags=[...])
client.agents.passages.search(agent_id=..., query=...)
client.agents.delete(agent_id=...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Vault and Letta client modules with integration tests</name>
  <files>
    hazn_platform/core/vault.py,
    hazn_platform/core/letta_client.py,
    tests/test_vault.py,
    tests/test_letta.py
  </files>
  <behavior>
    - Test: store_secret("test/creds", {"api_key": "sk-test-123"}) stores and read_secret("test/creds") retrieves the same data
    - Test: get_vault_client() returns an authenticated hvac.Client (client.is_authenticated() == True)
    - Test: After storing a secret, creating a VaultCredential with vault_secret_id="test/creds" and then reading it back via read_secret(credential.vault_secret_id) returns the original data
    - Test: get_letta_client() returns a connected Letta client
    - Test: Create a test agent via Letta SDK, verify agent.id is returned
    - Test: Insert an archival passage into the test agent, search for it, verify results contain the passage
    - Test: Clean up (delete test agent) succeeds
  </behavior>
  <action>
    1. Create `hazn_platform/core/vault.py`:
       ```python
       import hvac
       from django.conf import settings

       def get_vault_client() -> hvac.Client:
           """Get authenticated Vault client."""
           client = hvac.Client(url=settings.VAULT_ADDR, token=settings.VAULT_TOKEN)
           assert client.is_authenticated(), "Vault client is not authenticated"
           return client

       def store_secret(path: str, data: dict) -> str:
           """Store a secret in Vault KV v2. Returns the path."""
           client = get_vault_client()
           client.secrets.kv.v2.create_or_update_secret(
               path=path, secret=data, mount_point="secret"
           )
           return path

       def read_secret(path: str) -> dict:
           """Read a secret from Vault KV v2."""
           client = get_vault_client()
           response = client.secrets.kv.v2.read_secret_version(
               path=path, mount_point="secret"
           )
           return response["data"]["data"]
       ```

    2. Create `hazn_platform/core/letta_client.py`:
       ```python
       from letta_client import Letta
       from django.conf import settings

       def get_letta_client() -> Letta:
           """Get configured Letta client for self-hosted server."""
           return Letta(
               base_url=settings.LETTA_BASE_URL,
               api_key=settings.LETTA_SERVER_PASSWORD,
           )
       ```

    3. IMPORTANT: Before tests can use Vault, the VAULT_TOKEN Django setting must have the actual root token from `.vault-keys`. Either:
       - Read .vault-keys in conftest.py and set settings.VAULT_TOKEN dynamically, OR
       - Ensure .envs/.local/.django has the VAULT_TOKEN set (may require running vault-init first and copying the token)
       The recommended approach: create a conftest.py fixture that reads ROOT_TOKEN from .vault-keys and patches settings.VAULT_TOKEN.

    4. Write `tests/test_vault.py`:
       - Use @pytest.mark.django_db where needed
       - Test store and retrieve round-trip
       - Test creating a VaultCredential model instance with vault_secret_id, then using read_secret with that ID
       - NOTE: These are integration tests requiring a running Vault. Mark them appropriately (e.g., pytest.mark.integration or just rely on the Docker test runner)

    5. Write `tests/test_letta.py`:
       - Test client creation
       - Test agent creation with memory blocks (use a simple model like "openai/gpt-4o-mini" or whatever model is configured -- check what OPENAI_API_KEY or ANTHROPIC_API_KEY is available on the Letta server)
       - Test archival passage insert and search
       - Test cleanup (delete agent)
       - NOTE: Agent creation in Letta requires a valid LLM API key on the Letta server. If no LLM API key is configured, these tests will fail. Ensure OPENAI_API_KEY or ANTHROPIC_API_KEY is set in the Letta container's environment. If neither is available, the validate_letta management command (Task 2) provides a manual fallback.
       - IMPORTANT: Check the actual Letta SDK API carefully. The `letta-client` package API may differ from older `letta` package. Verify method names (e.g., `client.agents.create()` vs `client.create_agent()`). The research doc uses `client.agents.create()` and `client.agents.passages.insert()` -- verify these are correct.

    6. Handle the Letta model configuration question: The Letta server needs an LLM API key to create agents. Set OPENAI_API_KEY or ANTHROPIC_API_KEY in .envs/.local/.letta (or directly on the letta service in docker-compose). If you use Anthropic, the model string is `anthropic/claude-sonnet-4-5-20250929` or similar. If OpenAI, use `openai/gpt-4o-mini`. Default to whichever API key the user has available -- add both as optional env vars on the Letta service.
  </action>
  <verify>
    <automated>docker compose -f docker-compose.local.yml run --rm django pytest tests/test_vault.py tests/test_letta.py -x -v</automated>
  </verify>
  <done>
    Vault integration works: store_secret and read_secret round-trip successfully. VaultCredential model stores path reference only. Letta integration works: agent creation, archival insert, and semantic search all succeed via letta-client SDK. All integration tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create seed data command and service validation script</name>
  <files>
    hazn_platform/core/management/__init__.py,
    hazn_platform/core/management/commands/__init__.py,
    hazn_platform/core/management/commands/seed_dev_data.py,
    hazn_platform/core/management/commands/validate_letta.py,
    scripts/validate_services.sh
  </files>
  <action>
    1. Create `hazn_platform/core/management/commands/seed_dev_data.py`:
       - Create a test agency: "Acme Digital Agency" with house_style and methodology JSON
       - Create 2-3 end-clients under the agency: e.g., "TechStartup Inc" (with competitors list), "Green Energy Co", "Local Bakery"
       - Create brand voice for at least one end-client (content field with marketing tone description, no embedding yet -- leave null)
       - Create sample keywords for one end-client (3-5 keywords with search_volume, difficulty, intent)
       - Create a test Vault secret: call store_secret("agencies/acme-digital/ga4", {"api_key": "test-ga4-key-placeholder"}) and create a VaultCredential record pointing to that path
       - Create a sample campaign and decision for one end-client
       - Print summary of created objects: "Created: 1 agency, 3 end-clients, 1 brand voice, 5 keywords, 1 vault credential, 1 campaign, 1 decision"
       - Make idempotent: check if seed data exists before creating (e.g., check if "Acme Digital Agency" exists)

    2. Create `hazn_platform/core/management/commands/validate_letta.py`:
       - Get Letta client
       - Create a test agent named "hazn-test-agent" with persona and human memory blocks
       - Insert a test archival passage: "Test archival memory passage for Hazn validation"
       - Search archival memory for "Hazn validation"
       - Print number of search results
       - Clean up: delete the test agent
       - Print success message with green text (self.style.SUCCESS)

    3. Create `scripts/validate_services.sh`:
       - Check each service is reachable:
         - Postgres: pg_isready or psql connectivity check (via docker compose exec)
         - Vault: curl seal-status endpoint, check unsealed
         - Letta: curl health endpoint
         - Redis: docker compose exec redis redis-cli ping
         - Django: curl the Django health/admin endpoint
         - Next.js: curl http://localhost:3000
       - Print pass/fail for each service
       - Exit 0 if all pass, exit 1 if any fail
       - Make executable (chmod +x)

    4. Verify the full pipeline:
       - Run `make up` (services should be running from Plan 01)
       - Run `make migrate` (migrations from Plan 02)
       - Run `make seed` (this task's seed command)
       - Run `python manage.py validate_letta` (this task's Letta validation)
       - Run `bash scripts/validate_services.sh` (service health check)
       - Run `make test` (all tests including test_models, test_vault, test_letta)
  </action>
  <verify>
    <automated>docker compose -f docker-compose.local.yml run --rm django python manage.py seed_dev_data && docker compose -f docker-compose.local.yml run --rm django python manage.py validate_letta && docker compose -f docker-compose.local.yml run --rm django pytest -x -v</automated>
  </verify>
  <done>
    seed_dev_data creates test agency with end-clients, brand voice, keywords, vault credential, campaign, and decision. validate_letta successfully creates an agent, inserts archival memory, searches it, and cleans up. validate_services.sh confirms all Docker services are healthy. Full test suite passes.
  </done>
</task>

</tasks>

<verification>
Phase 1 Success Criteria verification (from ROADMAP.md):
1. `docker compose up` starts Postgres 17 (with pgvector), Letta, Vault, Redis, and Langfuse (external) -- all healthy
   -> `bash scripts/validate_services.sh` passes
2. Postgres schema accepts inserts/queries for all L2/L3 data tables
   -> `pytest tests/test_models.py` passes (from Plan 02)
   -> `python manage.py seed_dev_data` succeeds (creates real records)
3. Vault stores and retrieves a test secret; Postgres stores only the vault_secret_id reference
   -> `pytest tests/test_vault.py` passes
4. Letta server accepts agent creation and archival memory operations via SDK
   -> `pytest tests/test_letta.py` passes
   -> `python manage.py validate_letta` passes
</verification>

<success_criteria>
- vault.py store_secret/read_secret round-trip works against running Vault
- letta_client.py get_letta_client connects to running Letta server
- seed_dev_data command creates 1 agency, 2-3 end-clients, brand voice, keywords, vault credential, campaign, decision
- validate_letta command creates agent, inserts archival, searches archival, deletes agent
- validate_services.sh confirms all services healthy
- Full test suite (test_models + test_vault + test_letta) passes via `make test`
- All 4 Phase 1 success criteria from ROADMAP are met
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md`
</output>
